---
name: demo-skill
description: 在演示 skill 创建模式时使用。这是一个展示正确结构和约定的参考 skill。
---

# Demo Skill

这是一个示例 skill，演示如何编写 Claude Code skill。它作为插件开发者的参考。

## 触发

用户询问 skill 结构，或提及 "demo skill" 或 "example skill" 时。

## Purpose

此 skill 展示了 Claude Code skill 的正确格式和结构，包括：
- YAML frontmatter 字段
- Trigger 描述格式
- Markdown 主体中的说明
- 如何引用支持文件

## 示例用法

当用户询问 skill 结构或提及 "demo skill" 或 "example skill" 时，此 skill 会自动激活。

此 skill 是 example-plugin 模板的一部分，展示 skills 如何集成到插件 marketplace 项目中。
